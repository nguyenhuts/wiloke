<?php 
if ( apply_filters('pi_is_section_enable', 'aboutus') ) :
?>
<?php  
do_action("pi_hercules_before_about"); 
$background = apply_filters('pi_get_background', 'aboutus');
?>

  <section id="aboutus" class="story" data-background='<?php echo esc_attr($background) ?>'>
    <div class="container">

      <div class="wellcome st-heading text-center">
          <?php apply_filters( 'pi_the_heading', 'aboutus', 'welcome' ); ?>
          <?php apply_filters( 'pi_the_description', 'aboutus' ); ?>
      </div>
     
      
      <?php 
        $getID = apply_filters('pi_get_data', 'aboutus');

        if ( $getID ) : 
          $piaData = get_post_meta($getID, "_pi_aboutus", true);
      ?>
      <div class="row">
        <div class="col-md-10 col-md-offset-1">

         
            <?php isset($piaData['title']) && !empty($piaData['title']) ?    printf(__(('<div class="st-heading text-center"><h3 class="h3">%s</h3></div>'), piCore::LANG), ($piaData['title']) )  : "";  ?>      
            <?php isset($piaData['description']) && !empty($piaData['description']) ?    printf(__(('<div class="st-slogan text-center"><p>%s</p></div>'), piCore::LANG), ($piaData['description']) )  : "";  ?>      
          
          <div class="st-body">
            <div class="story-item">
                <div class="row">

                  <div class="col-xs-5 col-sm-3">
                    <?php 
                      if ( isset($piaData['photo']) && !empty($piaData['photo']) ) : 
                    ?>
                        <div class="image-wrap image-shadow">
                          <img class="img-circle" src="<?php echo esc_url(wp_get_attachment_url($piaData['photo'])) ?>"  alt="<?php echo get_post_meta($piaData['photo'], '_wp_attachment_image_alt', true); ?>">
                        </div>
                    <?php 
                      endif; 
                    ?>
                  </div>

                  <div class="col-sm-9">
                    <div class="story-content">
                    <?php isset($piaData['intro']) && !empty($piaData['intro']) ? printf( __( '<p class="desc_title_section">%s</p>' , piCore::LANG),  wp_unslash($piaData['intro']) )  : "";  ?>     
                    <?php isset($piaData['email']) && !empty($piaData['email']) ?    printf( __('<div class="email"><a class="h-btn" href="mailto:%s">%s</a></div>', piCore::LANG), $piaData['email'], wp_unslash($piaData['email']) )  : "";  ?>   
                    </div>  
                  </div>

                </div>
            </div>
          </div>

        </div>
      </div>
    <?php 
      endif; 
    ?>
    </div>
  </section>
  
<?php 
  do_action("pi_hercules_after_about");
?>
<?php 
endif;
?>